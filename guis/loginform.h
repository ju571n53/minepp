#ifndef LOGINFORM_H
#define LOGINFORM_H

#include <QWidget>

class loginForm : public QWidget
{
    Q_OBJECT
public:
    explicit loginForm(QWidget *parent = 0);
    
signals:
    
public slots:
    
};

#endif // LOGINFORM_H
